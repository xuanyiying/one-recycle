# ============================================================
# 云原生构建配置 - 入口文件
# ============================================================
#
# 所需密钥（通过 imports 导入，请在密钥仓库中配置 secrets.yml）：
#   DEPLOY_HOST  - 部署服务器 IP
#   DEPLOY_USER  - SSH 用户名
#   DEPLOY_KEY   - SSH 私钥
#   DOMAIN       - 域名（默认 backbuy.cn）
# ============================================================

include:
  - .ci/ci.yml
  - .ci/deploy.yml
