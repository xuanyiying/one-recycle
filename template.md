在如今vibe coding快速发展的时代，开发一个高度模块化、可复用的全栈项目模板，包含移动端，服务端，管理端，完整清晰的项目结构模板，
深度挖掘用户需求，写成需求文档，包含以下主要基础功能：

用户端：
1.登录注册（支持微信，支付宝，抖音，快手，谷歌，apple 账号，手机号，邮箱验证码登录注册）；
2.个人中心(主题设置，头像管理，重置密码，消息通知， 订阅信息)；
3. 隐私协议，用户协议，版权声明等法律条款展示；
4.支付订阅功能；
5.ai chat 功能
6.国际化（支持多语言）；
7.内容管理系统（文章，视频，图片，音频，文件等）；
8.搜索功能（全文搜索，分类搜索，标签搜索等）；
9.推送通知（消息通知，系统通知，营销通知等）；
10.反馈与支持（用户反馈，常见问题，在线客服等）；

管理端：

1.管理员登录；
2.ai 配置（角色，提示词，模型，api key 等）；
3.用户管理（用户列表，用户详情，用户删除，用户禁用，用户恢复，用户导出）；
4.订阅管理（订阅列表，订阅详情，订阅删除，订阅禁用，订阅恢复，订阅导出）；
5.数据统计（用户统计，订阅统计，活跃用户统计，付费用户统计）；
6.系统设置（网站信息，支付配置，ai 配置，邮件配置）；
7.日志管理（操作日志，登录日志，错误日志）；
8.插件服务（插件安装，插件卸载，插件启用，插件禁用，插件更新）；
9. 插件市场：
    插件列表展示（插件名称，插件描述，插件作者，插件版本，插件下载量）；
    插件详情展示（插件名称，插件描述，插件作者，插件版本，插件下载量，插件使用说明）；
    插件搜索功能（根据插件名称，插件描述，插件作者进行搜索）；
    插件安装功能（点击安装按钮，服务端自动安装插件依赖，重启服务端）；
    插件卸载功能（点击卸载按钮，服务端自动卸载插件依赖，重启服务端）；
    插件启用功能（点击启用按钮，服务端启用插件，重启服务端）；
    插件禁用功能（点击禁用按钮，服务端禁用插件，重启服务端）；
    插件更新功能（点击更新按钮，服务端自动更新插件依赖，重启服务端）；
服务端：

1. 用户服务（用户注册，用户登录，用户信息，用户修改，用户删除）；
2. 订单服务（订单创建，订单查询，订单修改，订单删除）；
3. 支付服务（支付创建，支付查询，支付修改，支付删除）；
5. 调度服务（任务调度，定时任务，任务查询，任务删除）；
6. 通知服务（邮件通知，短信通知，推送通知）；
7. 日志服务（操作日志，错误日志，登录日志）；
8. 报表服务（用户报表，订单报表，支付报表）；
9. 插件服务（插件安装，插件卸载，插件启用，插件禁用，插件更新）；
10. 插件市场：
    插件列表展示（插件名称，插件描述，插件作者，插件版本，插件下载量）；
    插件详情展示（插件名称，插件描述，插件作者，插件版本，插件下载量，插件使用说明）；
    插件搜索功能（根据插件名称，插件描述，插件作者进行搜索）；
    插件安装功能（点击安装按钮，服务端自动安装插件依赖，重启服务端）；
    插件卸载功能（点击卸载按钮，服务端自动卸载插件依赖，重启服务端）；
    插件启用功能（点击启用按钮，服务端启用插件，重启服务端）；
    插件禁用功能（点击禁用按钮，服务端禁用插件，重启服务端）；
    插件更新功能（点击更新按钮，服务端自动更新插件依赖，重启服务端）；
技术栈：
    前端：React Native + Expo + Redux + TypeScript + i18next + Axios + Jest
    管理端：React + Ant Design + Redux + TypeScript + i18next + Axios + Jest
    后端：NestJS + TypeScript + Prisma + PostgreSQL + gRPC + JWT + Jest
    容器化：Docker + Docker Compose
技术实现：
    高度模块化设计，前后端分离架构；
    完整的API设计文档，使用OpenAPI规范；
    自动化测试覆盖率达到80%以上；
    CI/CD流水线配置，实现自动化构建、测试、部署；
    插件及模块功能采用管理端注册方式， 移动端安装包，服务端自动安装依赖；
部署文档和使用说明；
    完整的README文档，包含项目介绍，技术栈，安装步骤，运行步骤，测试步骤，贡献指南等。

